<% include ../partials/header %>

<div class="row">
  <div class="col-md-3">
    <p class="lead">Yelp Camp</p>
    <ul class="list-group">
      <li class="list-group-item active">Info 1</li>
      <li class="list-group-item">Info 2</li>
      <li class="list-group-item">Info 3</li>
    </ul>
  </div>
  <div class="col-md-9">
    <div class="card mb-3">
      <img src="<%= campground.image %>" alt="<%= campground.name %>" class="card-img-top">
      <div class="card-body">
        <div class="d-flex justify-content-between">
          <h4 class="card-title"><a href="#"><%= campground.name %></a></h4>
          <h4 class="card-title">$9.00/night</h4>
        </div>
        <p class="card-text"><%= campground.description %></p>
      </div>
    </div>
    <div class="card bg-light">
      <div class="text-right">
        <a href="/campgrounds/<%= campground._id %>/comments/new" class="btn btn-success m-3">Leave a review</a>
      </div>
      <% campground.comments.map(comment => { %>
      <div class="row">
        <div class="col mx-3">
          <div class="d-flex justify-content-between">
            <strong><%= comment.author %></strong><span class="text-secondary">10 days ago</span>
          </div>
          <p><%= comment.text %></p>
        </div>
      </div>
      <% }) %>
    </div>
  </div>
</div>

<% include ../partials/footer %>
